function add(x,y) {
    return x + y;
}

const two = 2;
const result = add(two, 3);
const r2 = add(10,10);

// Anonymous function
const subtract = function(x, Y) {
    return x - Y;
}

const subtractResult = subtract(10,2);

//Anonyomous Arrow functions


const divide = (x, y) => x / y;